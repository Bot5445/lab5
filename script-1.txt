# Скрипт для тестирования команды execute_script
# Добавляет элемент, удаляет, сохраняет в другой файл и запускает сам себя (рекурсия)

# Показываем информацию о коллекции до изменений
info

# Добавляем новый элемент с интерактивным вводом полей
insert null

# Сохраняем коллекцию в файл file2.csv
save file_test

# Показываем все элементы после добавления
show

# Выводим сумму высот
sum_of_height

# Запускаем этот же скрипт повторно (для проверки рекурсии)
execute_script script-2.txt

# Удаляем элемент с ключом меньше 100000000 (если есть)
remove_lower_key 100000000

# Завершаем (раскомментировать для выхода)
# exit